<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#11111B"
    android:padding="12dp">

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:visibility="gone"/>

    <!-- Status: berhasil / partial / game over -->
    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Solving..."
        android:textColor="#A6E3A1"
        android:textSize="13sp"
        android:gravity="center"
        android:paddingBottom="2dp"/>

    <!-- Lines cleared score -->
    <TextView
        android:id="@+id/tvScore"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="#FAB387"
        android:textSize="13sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="2dp"/>

    <!-- Step indicator -->
    <TextView
        android:id="@+id/tvStep"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Step 0 / 0"
        android:textColor="#6C7086"
        android:textSize="12sp"
        android:gravity="center"
        android:paddingBottom="8dp"/>

    <com.blockblast.solver.GridView
        android:id="@+id/solutionGridView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="12dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/btnRestart"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:text="Reset"
            android:textSize="13sp"
            android:backgroundTint="#313244"
            android:textColor="#CDD6F4"
            android:layout_marginEnd="4dp"
            android:enabled="false"/>

        <Button
            android:id="@+id/btnPlayPause"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:text="Play"
            android:textSize="13sp"
            android:backgroundTint="#89B4FA"
            android:textColor="#11111B"
            android:textStyle="bold"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:enabled="false"/>

        <Button
            android:id="@+id/btnNext"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:text="Next"
            android:textSize="13sp"
            android:backgroundTint="#45475A"
            android:textColor="#CDD6F4"
            android:layout_marginStart="4dp"
            android:enabled="false"/>
    </LinearLayout>

    <!-- Tombol Next Round — muncul setelah semua piece selesai ditempatkan -->
    <Button
        android:id="@+id/btnNextRound"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="10dp"
        android:text="🎯  Next Round — Pilih Piece Baru"
        android:textSize="14sp"
        android:textStyle="bold"
        android:backgroundTint="#A6E3A1"
        android:textColor="#11111B"
        android:visibility="gone"/>

</LinearLayout>
